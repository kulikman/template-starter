---
description: Fix all critical — исправить все нарушения CRITICAL из audit/rules.json
---

1. Открой `audit/rules.json` и выбери все правила с `"severity": "CRITICAL"`.
2. Просканируй проект и исправь каждое реальное нарушение в коде/конфигах (не снижай уровень требований без записи в `audit/conflicts.log`).
3. Если правило неприменимо — отметь это кратко и перейди к следующему.
4. Запусти `pnpm lint` и `pnpm tsc --noEmit`; устрани ошибки.
5. По желанию добавь краткую запись в `audit/audit-history.json` с итогом фикса.
